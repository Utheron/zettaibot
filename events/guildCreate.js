/**
* @from ZettaiBot
*/

// #############################################
// # WHEN THE BOT IS ADDED TO A GUILD
// #############################################
module.exports = (bot, guild) => {
    console.log(`(｡･‿･｡) ${guild.name} just added me to their Guild`);
};